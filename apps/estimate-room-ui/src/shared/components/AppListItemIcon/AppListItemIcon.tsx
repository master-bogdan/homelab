import { ListItemIcon } from '@mui/material';
import type { ListItemIconProps } from '@mui/material';

export type AppListItemIconProps = ListItemIconProps;

export const AppListItemIcon = ListItemIcon;
