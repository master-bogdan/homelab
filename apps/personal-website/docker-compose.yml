services:
  retro-site:
    image: ghcr.io/yourname/retro-terminal-site:latest
    container_name: retro-terminal-site
    restart: unless-stopped
    environment:
      - PORT=3000
    ports:
      - "3000:3000" # or behind your reverse proxy
    # If you use an external reverse proxy, remove ports and attach to the same network
    # networks:
    #   - proxy
# networks:
#   proxy:
#     external: true