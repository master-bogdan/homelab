apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-authentik
spec:
  hostnames:
    - auth.example.invalid
  parentRefs:
    - name: homelab-gw
      namespace: networking
      sectionName: https
