apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: opensearch-dashboards-auth-staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-staging
  target:
    name: opensearch-dashboards-auth-staging
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: dashboards_username
    - secretKey: password
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: dashboards_password
