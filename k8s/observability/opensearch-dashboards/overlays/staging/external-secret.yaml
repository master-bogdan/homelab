apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: opensearch-dashboards-auth-staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-staging
  target:
    name: opensearch-dashboards-auth-staging
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: dashboards_username
    - secretKey: password
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: dashboards_password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: opensearch-dashboards-oidc-staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-staging
  target:
    name: opensearch-dashboards-oidc-staging
    creationPolicy: Owner
  data:
    - secretKey: client_id
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: oidc_client_id
    - secretKey: client_secret
      remoteRef:
        key: kv/staging/opensearch-dashboards
        property: oidc_client_secret
