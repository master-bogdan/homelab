fullnameOverride: opensearch-dashboards-dev

opensearchHosts: "http://opensearch-cluster-master.observability.svc.cluster.local:9200"

opensearchSecurity:
  enabled: false

service:
  type: ClusterIP
  port: 5601

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

ingress:
  enabled: false
