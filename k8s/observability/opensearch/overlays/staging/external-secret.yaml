apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: opensearch-admin-staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-staging
  target:
    name: opensearch-admin-staging
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kv/staging/opensearch
        property: admin_password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: opensearch-oidc-staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-staging
  target:
    name: opensearch-oidc-staging
    creationPolicy: Owner
  data:
    - secretKey: client_id
      remoteRef:
        key: kv/staging/opensearch
        property: oidc_client_id
    - secretKey: client_secret
      remoteRef:
        key: kv/staging/opensearch
        property: oidc_client_secret
