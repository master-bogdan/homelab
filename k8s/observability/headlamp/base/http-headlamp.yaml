apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-headlamp
spec:
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: headlamp
          port: 80
