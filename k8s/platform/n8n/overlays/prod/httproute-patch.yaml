apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-n8n
spec:
  hostnames:
    - n8n.example.invalid
  parentRefs:
    - name: homelab-gw
      namespace: networking
      sectionName: https
