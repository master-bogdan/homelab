apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-seaweedfs-dashboard
spec:
  hostnames:
    - seaweedfs.example.invalid
  parentRefs:
    - name: homelab-gw
      namespace: networking
      sectionName: https
