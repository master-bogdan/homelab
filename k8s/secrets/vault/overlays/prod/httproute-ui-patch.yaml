apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-vault-ui
spec:
  hostnames:
    - vault.example.invalid
  parentRefs:
    - name: homelab-gw
      namespace: networking
      sectionName: https
